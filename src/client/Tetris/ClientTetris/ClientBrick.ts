import { ServerField } from "../ServerTetris/ServerField.js";
import { Stone } from "./Stone.js";

// export class ClientBrick extends Phaser.Physics.Arcade.Group {

//     constructor(scene: Phaser.Scene, field: Field, id: number) {
//         super(scene.physics.world, scene);

//     }


//     generateBrick(stones: boolean[][],field: Field,id: number,xC: number, yC: number) {
//         super.destroy(true);

//         for (let x = 0; x < stones.length; x++) {
//             for (let y = 0; y < stones.length; y++) {
//                 if (stones[x][y]) {
//                     this.add(new Stone(field, id, x + xC, y + yC));
//                 }
//             }

//         }
//     }
// }
